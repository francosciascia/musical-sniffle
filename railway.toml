# Railway: New Project → Deploy from GitHub repo
# Agregá un plugin Postgres y las variables de abajo.
# https://railway.app

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
