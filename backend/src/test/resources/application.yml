spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false

app:
  jwt:
    secret: test-jwt-secret-key-minimo-32-chars!!!!!!
    expiration-ms: 3600000
  estacionamiento:
    plaza-obligatoria: false
