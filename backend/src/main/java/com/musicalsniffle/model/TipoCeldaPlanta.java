package com.musicalsniffle.model;

public enum TipoCeldaPlanta {
    /** Área donde se pueden ubicar plazas. */
    FORMA,
    /** Calle / pasillo interno (sin plazas). */
    CIRCULACION,
    /** Acceso de ingreso. */
    ENTRADA,
    /** Acceso de egreso. */
    SALIDA,
    /** Columna, pared u obstáculo. */
    OBSTACULO
}
